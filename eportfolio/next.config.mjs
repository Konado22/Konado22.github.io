/** @type {import('next').NextConfig} */
const nextConfig = {
  //nexjs config files, NEXTJS IS AMAZING
    output: "export",
    //basePath:'',
    images: {
        unoptimized: true,
      }
};

export default nextConfig;
